export class Ranking {
  rank: number;
  totalpoints: number;
  name: string;
  per16points: number;
  per8points: number;
  per4points: number;
  per2points: number;
  per1points: number;
  goalpoints: number;
}
